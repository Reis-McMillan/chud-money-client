// Dev stub. In the Docker image nginx answers /config.js itself with values
// from the container environment (see nginx.conf); this file only keeps the
// script tag from 404ing under `npm run dev`, where VITE_* applies instead.
window.__CHUD_CONFIG__ = {}
